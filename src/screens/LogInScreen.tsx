import React from "react"
import LoginButton from "../auth0/LoginButton"

const loginScreen = () => {
    return (
        <div>
            <LoginButton />
        </div>
    )
}

export default loginScreen